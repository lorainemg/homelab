# Token for the Cloudflare Tunnel that exposes the reverse proxy to the internet.
# Create one at: Cloudflare Zero Trust -> Networks -> Tunnels
CLOUDFLARE_TUNNEL_TOKEN=changeme

# Base domain all services are published under (e.g. grafana.<DOMAIN_BASE>)
DOMAIN_BASE=example.com

TZ=America/New_York
