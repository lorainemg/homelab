{
  "skill_name": "adding-a-stack",
  "evals": [
    {
      "id": 0,
      "name": "adds-new-stack-default-path",
      "prompt": "I want to add Paperless-ngx to the homelab. It needs Postgres and a data directory. Set up whatever's needed so it deploys like everything else.",
      "expected_output": "Chooses method 1 (Komodo git Stack). Names project_name equal to the directory name and explains why. Commits .env.example, never .env. Places the real .env at /etc/komodo/stacks/paperless/paperless/.env. Creates a GitHub webhook as a separate step. Does not add anything to deploy.yml.",
      "files": [],
      "assertions": [
        "Chooses a Komodo git Stack rather than adding the stack to .github/workflows/deploy.yml",
        "Sets project_name equal to the repo directory name AND explains that a mismatch creates fresh empty volumes",
        "Commits .env.example only, and places the real .env on the host under /etc/komodo/stacks/",
        "Treats creating the GitHub webhook as its own step, separate from webhook_enabled on the Stack",
        "Does not modify .github/workflows/deploy.yml"
      ]
    },
    {
      "id": 1,
      "name": "diagnoses-missing-webhook",
      "prompt": "I added a bookstack stack last week and it's running fine. I pushed a change to its compose file this morning and nothing happened on the server. CI was green. What's wrong?",
      "expected_output": "Identifies the missing GitHub webhook as the leading cause - webhook_enabled on the Komodo Stack opens the listener but GitHub still needs its own webhook per stack. Does not conclude CI is broken.",
      "files": [],
      "assertions": [
        "Names the missing GitHub-side webhook as the leading diagnosis",
        "Distinguishes webhook_enabled on the Komodo Stack from the webhook GitHub must send",
        "Does not blame CI, the paths-filter, or a build failure as the primary cause",
        "Gives a concrete command or API call that would confirm the diagnosis"
      ]
    },
    {
      "id": 2,
      "name": "refuses-tunnel-onto-komodo",
      "prompt": "Let's move the cloudflared tunnel into Komodo so it's managed like everything else. It's annoying that it's the only thing I have to bring up by hand.",
      "expected_output": "Declines and explains the blast-radius reason: anything deployable can be broken by a deploy, and the tunnel is the only route in - including to Komodo's own UI, which is published through it. Identifies this as method 4 (host-managed, never deployed).",
      "files": [],
      "assertions": [
        "Declines to move the tunnel onto Komodo rather than carrying out the request",
        "Gives the blast-radius reason: a bad deploy of the stack holding the tunnel removes the path used to repair it",
        "Notes that Komodo's own UI is reached through the tunnel, so the failure is self-locking",
        "Identifies this as the host-managed / never-deployed category rather than an oversight"
      ]
    },
    {
      "id": 3,
      "name": "mounts-config-from-checkout",
      "prompt": "Add Uptime Kuma, with its config mounted from the repo so I can edit the check definitions in git.",
      "expected_output": "See assertions.",
      "files": [],
      "assertions": [
        "Sets webhook_force_deploy: true and explains the default check only compares the compose file",
        "Adds a post_deploy command restarting the affected service, because compose leaves unchanged services alone",
        "Mounts the directory, not a single file",
        "Warns that getting this wrong fails silently - the stack deploys and stays healthy while edits never arrive"
      ]
    },
    {
      "id": 4,
      "name": "built-image-ordering",
      "prompt": "I wrote a small Go service in this repo that has to be built into an image before it can run. Get it deploying.",
      "expected_output": "See assertions.",
      "files": [],
      "assertions": [
        "Builds and triggers the deploy in the SAME CI job, strictly ordered",
        "Explains the race: a separate deploy path can land the previous :latest and report success",
        "Tags with both :latest and the commit SHA",
        "Uses a positive paths-filter glob rather than a negated !pattern"
      ]
    },
    {
      "id": 5,
      "name": "foreign-repo-deploy",
      "prompt": "My side project lives in its own repo with its own CI. I want it deployed on the homelab without its compose file living in this repo.",
      "expected_output": "See assertions.",
      "files": [],
      "assertions": [
        "Reaches for a file_contents Stack rather than vendoring the compose file into this repo",
        "Creates a Komodo service user scoped to that one stack, not an admin key",
        "Pins project_name to the live project name",
        "Notes that /execute returning 2xx means accepted, not deployed - poll to confirm"
      ]
    }
  ],
  "notes": "Iteration 1 (evals 0-2) and iteration 2 (evals 3-5) both came out non-discriminating on correctness - the controls passed too, because this repo documents the answers. The measured benefit is cost: -21% to -25% tokens and up to -52% tool calls. Eval 3's post_deploy assertion is too broad and needs rewriting: a service that watches its own config (Caddy, Gatus) must NOT get a restart."
}
