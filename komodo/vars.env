# Values rendered into komodo/stacks.toml before it is pushed into the sync.
# Edit here, nowhere else; the sync job in .github/workflows/deploy.yml and
# scripts/bootstrap.sh both read this file.
HOMELAB_LAN_IP=172.20.3.194
