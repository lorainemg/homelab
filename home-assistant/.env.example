# Root of the data disk where service state lives
DATA_ROOT=/data

# Fine-grained PAT (contents read/write on this repo only) used by
# config-backup to push UI-made yaml edits back to git
HOMELAB_PUSH_TOKEN=changeme
